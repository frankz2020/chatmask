# Only needed for standalone (non-OpenClaw) use.
# When running via the OpenClaw skill, the agent analyses images directly
# and passes bounding boxes via --bbox-json, so no API key is required.
OPENROUTER_API_KEY=your-api-key-here
